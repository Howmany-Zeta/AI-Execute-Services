# Pre-commit hooks configuration for AIECS
# Install: poetry run pre-commit install
# Run manually: poetry run pre-commit run --all-files

repos:
  # -------------------------------------------------------------------------
  # insert-license — copyright header compliance
  # Inserts / verifies the IRETBL license header in every Python source file
  # under aiecs/ and examples/. Uses # comment style so output is valid Python.
  # -------------------------------------------------------------------------
  - repo: https://github.com/Lucas-C/pre-commit-hooks
    rev: v1.5.6
    hooks:
      - id: insert-license
        name: insert-license (copyright header)
        files: '^(aiecs|examples)/.*\.py$'
        args:
          - --license-filepath
          - LICENSE_HEADER.txt
          - --comment-style
          - "#"
          - --no-extra-eol

  - repo: local
    hooks:
      # -----------------------------------------------------------------------
      # black — code formatter
      # Formats aiecs/ in-place; pre-commit fails if files were modified,
      # prompting the developer to re-stage and commit.
      # -----------------------------------------------------------------------
      - id: black
        name: black (code formatter)
        entry: poetry run black
        language: system
        types: [python]
        files: '^aiecs/(?!scripts/)'
        description: "Formats Python code with black (line-length=200, see pyproject.toml)"

      # -----------------------------------------------------------------------
      # flake8 — style & lint checker
      # Reads config from .flake8 (max-line-length=200, E203/W503 ignored).
      # -----------------------------------------------------------------------
      - id: flake8
        name: flake8 (style & lint)
        entry: poetry run flake8
        language: system
        types: [python]
        files: '^aiecs/(?!scripts/)'
        description: "Checks PEP8 compliance with flake8 (config in .flake8)"

      # -----------------------------------------------------------------------
      # mypy — static type checker
      # Reads config from [tool.mypy] in pyproject.toml.
      # pass_filenames=false so mypy analyses the whole package at once,
      # enabling cross-module type inference.
      # -----------------------------------------------------------------------
      - id: mypy
        name: mypy (type checker)
        entry: poetry run mypy
        language: system
        types: [python]
        files: '^aiecs/(?!scripts/)'
        pass_filenames: false
        description: "Runs mypy static type checking on aiecs/ (config in pyproject.toml [tool.mypy])"

      # -----------------------------------------------------------------------
      # deptry — declared vs imported dependencies (PEP 621)
      # -----------------------------------------------------------------------
      - id: deptry
        name: deptry (dependency consistency)
        entry: poetry run deptry aiecs
        language: system
        pass_filenames: false
        files: '^(pyproject\.toml|aiecs/.*\.py)$'
        description: "Checks pyproject.toml dependencies match aiecs/ imports (DEP001/DEP002)"

