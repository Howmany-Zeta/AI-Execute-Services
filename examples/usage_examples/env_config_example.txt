# AIECS 环境配置示例
# 复制此文件为 .env 并填入实际值

# =====================================
# LLM 提供商配置（至少配置一个）
# =====================================

# OpenAI 配置
OPENAI_API_KEY=sk-your-openai-api-key-here

# Google Vertex AI 配置
VERTEX_PROJECT_ID=your-gcp-project-id
VERTEX_LOCATION=us-central1
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account.json

# xAI 配置（Grok）
XAI_API_KEY=your-xai-api-key-here

# =====================================
# 基础设施配置
# =====================================

# Redis 配置（消息队列）
CELERY_BROKER_URL=redis://localhost:6379/0

# PostgreSQL 数据库配置
# 连接模式：设置为 "local" 使用单独参数，设置为 "cloud" 使用连接字符串
# 默认：local
DB_CONNECTION_MODE=local

# 本地模式配置（当 DB_CONNECTION_MODE=local 时使用）
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_NAME=aiecs
DB_PORT=5432

# 云端模式配置（当 DB_CONNECTION_MODE=cloud 时使用）
# POSTGRES_URL=postgresql://user:password@host:port/database?sslmode=require

# =====================================
# 云存储配置（可选）
# =====================================

# Google Cloud Storage
GOOGLE_CLOUD_PROJECT_ID=your-gcp-project-id
GOOGLE_CLOUD_STORAGE_BUCKET=your-storage-bucket

# =====================================
# 服务配置
# =====================================

# CORS 允许的源（生产环境请设置具体域名）
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,https://yourdomain.com

# 服务端口（可选，默认 8000）
PORT=8000

# 开发模式重载（可选）
RELOAD=false
